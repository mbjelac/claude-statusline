# Claude Statusline

A customized Claude [status line](https://code.claude.com/docs/en/statusline).

## Usage

1. Fork & clone this repo
2. Tweak `statusline-command.sh`
3. Open your Claude settings (`~\.claude\settings.json`)
4. Set `statusLine.command` to absolute path of `statusline-command.sh`
